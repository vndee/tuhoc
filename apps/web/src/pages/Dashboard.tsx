/**
 * Placeholder for `/`. Real dashboard content (course cards, progress ring,
 * streak, 30-day chart) is Task 14's job — see spec §5 and Task 8's
 * `/stats` endpoint.
 */
export function Dashboard() {
  return (
    <div>
      <h1 className="ch-title">Bảng điều khiển</h1>
      <p className="ch-lede">Nội dung bảng điều khiển sẽ hiển thị ở đây.</p>
    </div>
  );
}

export default Dashboard;
