/**
 * Placeholder for `/login`. Real auth UI + API client is Task 12's job.
 */
export function Login() {
  return (
    <div>
      <h1 className="ch-title">Đăng nhập</h1>
      <p className="ch-lede">Biểu mẫu đăng nhập / đăng ký sẽ hiển thị ở đây.</p>
    </div>
  );
}

export default Login;
